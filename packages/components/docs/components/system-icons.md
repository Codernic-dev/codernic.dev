# System Icon Library

Complete minimalist stroke SVG icon set matching the Lucide aesthetic.

## Live React Component Render

<ReactSpecimen componentName="system-icons" />

## Usage

```tsx
import { IconCpu, IconTerminal, IconShield, IconZap } from '@codernic/components/atoms';

<IconCpu size={20} color="currentColor" />
<IconTerminal size={20} color="currentColor" />
<IconShield size={20} color="currentColor" />
```
